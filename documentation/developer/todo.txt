- fix setNumBuffers: currently only reaches the CUDA backend, OpenCL/Vulkan pick the value up only at backend creation, so calling it on those backends is a silent no-op. minimal fix: drop the CUDA special case and push the value once inside initialize(). long term: move numInputBuffers into the per-backend BackendConfig (like numOutputBuffers)
- python wheels (later): Windows binaries load without a Vulkan runtime because vulkan-1.dll is delay-loaded (src/CMakeLists.txt). Linux is expected to need no equivalent: auditwheel bundles libvulkan.so.1 into the wheel (the loader works without a driver and finds system ICDs via manifest files), and driverless machines then hit the runtime probe which reports Vulkan as unavailable. verify this when the first Linux wheel is built (note: auditwheel mangles bundled library names, so the probe's dlopen by soname finds the system loader, not the bundled copy. the reported result stays correct either way)
